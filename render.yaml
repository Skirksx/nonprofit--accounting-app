services:
  - type: web
    name: nonprofit-accounting-app
    env: node
    plan: free
    buildCommand: yarn install --production=false
    startCommand: yarn start
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: DATABASE_URL
        sync: false
